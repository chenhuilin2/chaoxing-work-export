# Changelog

## 3.0.18

### 变更

- **移除「附加解析」导出选项**（按用户决定）。该选项自 3.0.16 起已确认在导出链路里**完全没有被消费** —— 勾选它不改变任何导出内容，留着只会误导。本次连同 UI 复选框、`ExportOptions.includeAnalysis` / `ExportPreferences.includeAnalysis` 字段、设置解析（`settings-repository` 的兼容读取）、历史记录迁移（`history-repository`）、浏览器冒烟脚本一并移除。
  - **保留**：`Question.analysis` 的提取（`extractors/common-answer.ts` 的 `extractAnalysis`）与历史记录存储不变，只是不再有任何导出开关；存量设置与历史记录里多出来的 `includeAnalysis` 字段会被正常忽略（解析器逐字段取值，不做整体校验）。
  - 面板「导出文件」选项行现在是：附加答案 / 附加错题 / 题型内乱序 / 题库导入 / 按章节拆分文件。

### 修复

- **Word 多章节单文件：每一章的题号从 1 重新开始**。此前正文、答案页、错题页共用一个全局计数器，第 2 章的第一题会从第 1 章最后一道的序号继续往下排（用户样例文档里 5 章一路排到 13）。现在三处都改为**按章重新从 1 数起**：正文与答案页的编号仍然一一对应，错题页显示的就是该题在原章的题号。
  - 顺带修正：**按章节拆分文件**产出的单章文档本来就每份从 1 开始，改动后两条路径的编号规则终于一致。
- **Word 多章节单文件：题型大题头的序号按章重排，不再跳号**。大题头此前用的是全局固定的题型序号（单选=一、多选=二、填空=三、判断=四、简答=五），于是某章只要没有单选题，多选题就会顶着「二、多项选择题」、判断题顶着「四、判断题」（用户样例文档里两个章节都是这样）。现在序号按**本章实际有题目**的题型从「一」重新排：第二章只有多选 + 判断时就是「一、多项选择题」「二、判断题」。正文、答案页、错题页共用同一份映射，同一章里同一题型的大题头编号一致。
- **Word 题型大题头字号比章节标题小一号**（28 → 24 半磅，即 14pt → 12pt）。此前两者同为 28，大题头与章节标题视觉上无法区分；现在层级为 文档标题 32 > 章节标题 28（答案页/错题页的章节小标题 26）> 题型大题头 24 > 正文 22。**单章节模式同样生效**（单章节没有章节标题，此前的 28 只会把大题头顶到接近文档标题的层级）。

### 验证

- 单测 **112 → 113 例全绿**：`tests/word-export.test.cjs` 的多章节用例改为断言题号按章重启（`['1. ', '2. ', '1. ']`），答案页 / 错题页用例追加三处编号断言（正文 3 + 答案 3 + 错题 2 = `['1.','2.','1.','1.','2.','1.','1.','1.']`），新增 1 例断言「章内题型序号从一重排 + 大题头 24 比章节标题 28 小一号」；`tests/panel-view.test.cjs` 把「附加解析不依赖附加答案」那例换成「附加解析已整体移除、其余选项顺序不变」。
- **真实 docx 生成校验**（`.workbuddy/verify-word-docx.cjs`，真实 `docx` 库 + 拆 XML）新增 4 项断言并全部通过：章节大标题 28 半磅、题型大题头 24 半磅、某章没有单选时大题头是「一、多项选择题」且全文不出现「二、」「三、」跳号、题号按章从 1 重新开始；原有 13 项断言（拆分文件、分页数、答案页/错题页、题库导入）无回归。
- typecheck 与 ESLint（`src`）0 问题；真实快照回归（`.workbuddy/verify-options.cjs`）与跨文档验证（`.workbuddy/verify-cross-document.cjs`）全部通过。

### 说明

- `src/infrastructure/history-repository.ts` 存在既有的 prettier 格式偏差（`migrateLegacyResult` 里的三元表达式换行，HEAD 即不合规），本次未做全文件格式化。

## 3.0.17

### 修复

- **Word 导出勾选「按章节拆分文件」不起作用**。`export-service.ts` 的 Word 分支一直硬编码为「合并导出」（原注释即写着「Word 暂不支持分章节」），`splitByChapter` 只在 TXT / Markdown 分支被读取，因此勾上这个选项对 Word 完全无效果，多章节结果永远是一个文件、且**各章节的同题型题目被合并进同一个大题**（单选全排在一起、多选全排在一起，看不出题目属于哪一章）。

### 改进

- **Word 多章节的两种组织方式都实现了**（按用户选定方向）：
  - 勾选「按章节拆分文件」→ **一章一个 .docx**，命名 `基础名_1_章节名.docx`（与 TXT / Markdown 的既有行为一致），每个文件是一份完整的小卷子（自己的章节名标题 + 该章的题型分组 + 该章的答案页 / 错题页，题号从 1 重新开始）；
  - 不勾选时，**多章节的单文件内部按「章节 → 题型」分节**：每章一个居中大标题（除首章外先分页），章节内部再按题型分大题；答案页与错题页同样按章节给出小标题，题号全程连续以保证答案与题目编号对齐。单章节结果的输出与改动前完全一致。
  - 实现上把「拆几个文件、每个文件装哪些章节、文件名怎么起」抽成纯函数 `src/exporters/word-plan.ts`（不碰 docx），`word-exporter.ts` 改为接受 `sections` 列表渲染。这样拆分的判定可以被单测覆盖 —— 原 `word-exporter` 依赖 CDN 注入的全局 `docx`，在 Node 里加载不了，这段渲染代码此前**零覆盖**。
- **多章节提取的进度改为「已提取 N/M 章节」**。章节弹窗底部那行原来只显示 `${completed}/${total} · 状态：章节名`，计数不像「已完成多少章」；现在统一为 `已提取 3/54 章节 · 正在加载：导论4`（失败时带原因），并**同步到面板状态栏**，避免弹窗被关掉后看不到进度。下载完成提示在多文件时改为「已下载 N 个章节文件」，不再只说「Word 试卷已下载」。
- **设置弹窗三行开关的标签字重与上方设置行一致**（`允许拖动面板` / `记忆面板位置` / `打开窗口自动提取` 补上 `font-weight: 700`）。此前这三行用的是默认字重，与「界面主题」「提取快捷键」等粗体标签不统一。

### 验证

- 单测 **100 → 112 例全绿**。新增 `tests/word-plan.test.cjs` 6 例（单章节不拆、多章节单文件按章节分节且各章同题型不再混在一起、拆分后一章一文件与文件名序号、章节名非法字符替换与扩展名剥离、题库导入不插章节标题、文件名回退到结果标题）与 `tests/word-export.test.cjs` 5 例（把全局 `docx` 换成可检查的打桩实现，直接断言生成出来的段落序列：单章节不插章节标题、多章节两个 HEADING_2 分节且题号连续、拆分文件只装本章题目、答案页与错题页也按章节分节、题库导入不混入章节名）；`tests/panel-view.test.cjs` 新增 1 例（`已提取 N/M 章节` 的文案与状态栏同步、失败态标红）并补了开关行字重的样式断言。共用夹具抽到 `tests/export-fixtures.cjs`（文件名不以 `.test.cjs` 结尾，不会被当成用例收集）。
- **真实 docx 生成校验**（新增 `.workbuddy/verify-word-docx.cjs`，用真实 `docx` 库而非打桩）：拆分模式产出 2 个 9 KB 的 .docx，各自只含本章题目、各有一页答案；合并模式 1 个文件含两个章节标题、**3 处分页（正文章节间 + 答案页 + 错题页）**、正文里每章各一份单选大题头、单选题型头共 6 处（正文 2 + 答案页 2 + 错题页 2）；单章节模式仅 1 处分页；题库导入格式含 2 道单选 + 1 道多选且不混入章节名。生成的样例文档留在 `.workbuddy/word-check/`。
- typecheck 与 ESLint（`src`）0 问题；真实快照回归（`.workbuddy/verify-options.cjs`）全部通过（答题页 5/5/20、考试 56/36/144、外层壳章节名「导论1」、选项晚 300ms 采样 1131ms 补齐）。

### 说明

- 题库导入（智能导入）格式不认章节标题，因此**多章节时也不在单文件里插章节标题**，题目保持按题型连续排列；勾选「按章节拆分文件」时仍会拆成多个文件（每个文件只装一章的题目）。
- 本次把 `word-exporter.ts` 整体重写并做了 prettier 格式化（该文件改动量覆盖全文）；`export-service.ts` 与 `app-controller.ts` 未做全文件格式化，其既有的格式偏差（导入块换行导致的整体行号偏移）保持原样。
- 仍未处理（3.0.16 已记录）：`includeAnalysis`（附加解析）在导出链路里没有被消费。

## 3.0.16

### 改进

- **多章节选择弹窗：两个按钮改成一个全选框 + 右侧状态文本，并把弹窗加宽**。原来的「全选 / 取消全选」两个按钮合成一个「全选」框，它同时兼作选择状态的指示：全选时勾上、一个都没选时取消、只选了一部分显示为半选（`indeterminate`）；框右侧常驻 `已选 3 / 共 8 个章节`，勾选任意章节即刻刷新。原来放在列表下方的「共识别 N 个章节」文案并入这条状态，底部那行让给提取进度。弹窗宽度由 390px 放宽到 560px（长章节名不再频繁折行）。
- **「附加错题」与「附加解析」换位，且附加解析不再受附加答案/附加错题牵制**。两个选项在面板上的顺序调整为「附加答案 / 附加错题」一行、「附加解析 / 题型内乱序」一行，同类项相邻；`includeAnalysis` 此前被 `!(withAnswers || withWrong)` 强制禁用，现在只有「题库导入」会覆盖整套选项时才禁用。
  - 顺带发现（**本版未改动**）：`includeAnalysis` 目前在导出链路里**完全没有被消费** —— `Question.analysis` 已由提取器读出并存入历史记录，但 `exporters/legacy-bridge.ts` 未把它带进导出结构，TXT / MD / Word 三个导出器都不读这个开关，因此勾选它不会改变导出内容。是否要把解析写进导出（还是干脆移除这个选项）待定。
- **设置弹窗：开关组上移、快捷键设置下移，并放宽开关组行距**。行顺序调整为「界面主题 → 允许拖动面板 → 记忆面板位置 → 打开窗口自动提取 → 提取快捷键 → 隐藏快捷键」，快捷键用分隔线与上方开关组区分；开关行上下内边距由 9px 增至 13px，不再挤成一片。
- **「打开窗口自动提取」默认值由关闭改为打开**（`DEFAULT_SETTINGS.autoExtractOnLoad`）。只改默认值对**已保存过设置**的用户无效 —— 该字段早就被显式写入过（值为旧的默认值 `false`），会一直盖住新默认值，所以 `settings-repository` 增加了一次性升级：首次加载存量配置时把它补成新默认值并记下标记，此后用户在设置里主动关掉会被正常保存、不会被再次改回。

### 验证

- 单测 **94 → 100 例全绿**。新增 `tests/panel-view.test.cjs` 3 例（章节弹窗的全选框/半选态/状态文本与加宽 class、导出选项的顺序与「附加解析不依赖附加答案、题库导入仍覆盖」、设置弹窗的行顺序）与 `tests/settings-repository.test.cjs` 3 例（新默认值为打开、存量配置首次加载升级并写回、用户主动关掉后不被改回）。`tsconfig.test-build.json` 相应纳入 `src/ui/**`、`src/app-config.ts`、`src/infrastructure/settings-repository.ts` —— 面板挂在 Shadow DOM 上，linkedom 支持 `attachShadow`，因此面板交互可以在 Node 里按真实 DOM 语义验证。
- typecheck 与 ESLint（`src`）0 问题；真实快照回归（`.workbuddy/verify-options.cjs`）全部通过。

## 3.0.15

### 修复

- **章节测验「已完成 / 已批阅」视图「能提取到题干、提取不到选项」的真正根因**：用户回传的 3.0.14 诊断报告给出了决定性证据 —— 同一份现场里，**容器内 `.Zy_ulTop > li = 4`、`li[role="option"] = 4`，而提取器「选项总数 = 0」**。即选项节点**就在我们遍历的容器里**，只是**每个节点都解析成了空**，此前 3.0.10–3.0.14 关于「等得不够久」「选项在容器外」的判断都不成立。
  - 根因在 `rich-content.ts` 的富文本遍历门禁：`if (!(node instanceof Element)) return;`。脚本在顶层窗口运行时，会把**同源 iframe 的文档**一并遍历（`collectAccessibleDocuments`），而 iframe 里节点的构造器属于**它自己的 window**，`instanceof Element` 对它们一律判 false ⇒ 遍历**进不去任何子元素**。
  - 这个 bug 的签名恰好是本次现象：题干若落在容器的**直接文本节点**上还读得到（报告里的题干 `（）标志着…` 丢了嵌套在 `<span class="newZy_TItle">` 里的题型标记，正是旁证），而选项文字**一律嵌在 `<a>` / `<label>` / `<span>` 里**，就一条都读不到。真机报告与 `tests/yipi-view.test.cjs` 的模板完全吻合（题干是直接文本、选项文字嵌在 `<a>`）。
  - 修复：门禁改为按 `nodeType` 判定（`isElementNode()`），与节点属于哪个 window 无关；`extractBackgroundImages()` 的 `instanceof HTMLElement` 同样改为按能力判断（能否取到 `style`）。**同 realm 行为不变**（单文档页面、离线快照、既有 94 例用例全部照旧）。
  - 影响面比「选项」更大：跨层读 iframe 时，任何**嵌在子元素里的**题干、选项、答案、解析文本此前都会丢失；题干完全嵌在子元素里的模板甚至会整题消失。

### 改进

- **诊断报告新增「解析出选项 = N 条」与逐 `li` 的「原文 vs 管道」对照**：`解析出选项` 是提取管道真正读出的条数，与候选命中数对照即可把成因一分为二 —— 命中数 0 是选择器没命中；命中数 > 0 而解析数 0 是节点找到了却读不出文字。展开块同时打印未处理的 `textContent`（`li 原文`）与管道读出的文本（`li 管道`），两者不一致即证明文字在节点里、只是没被读到 —— 本次就是靠这一栏才能一轮定位，此前只能靠推断。
  - 报告首部补了「读法」说明；链路正常时（命中数、解析数都 > 0）不展开逐 li 文本，报告保持精简。

### 验证

- 单测 **88 → 94 例全绿**。新增 `tests/cross-document.test.cjs` 4 例（`tests/dom-setup.cjs` 新增 `withForeignConstructors()`，把全局 `Element` / `HTMLElement` 换成无关的类来还原「节点来自另一个 window」）：嵌套元素文本可读（`A毛泽东`）、选项完整提取（A–D）、题干里嵌套的题型标记不丢、背景图识别结果与同 realm 一致；`tests/dom-diagnostics.test.cjs` 新增 1 例（节点在、文字读不出时报告给出「原文 vs 管道」对照）并在既有用例中断言 `解析出选项`；`tests/yipi-view.test.cjs` 新增 1 例（已批阅视图跨 window 读取时选项仍完整）。
- 真实快照跨 window 对照（新增 `.workbuddy/verify-cross-document.cjs`）：`单章节内iframe详细.html` 同 realm 5 题 / 20 选项、跨 window 5 题 / 20 选项 / **缺选项选择题 0**；`考试页面.html` 56 题 / 144 选项一致。同一脚本用**旧门禁复算**：首个选项 `li` 读到「（空）」⇒ 旧实现下这些页面的选项必然全丢，机制得到实证。
- 真实快照回归（`.workbuddy/verify-options.cjs`）全部通过（答题页 5/5/20、考试 56/36/144、外层壳章节名「导论1」、选项晚 300ms 采样 1146ms 补齐）；typecheck 与 ESLint（`src`）0 问题。

### 说明

- 3.0.14 的「选项在外层单题块 / 按 qid 找回」兜底**保留**（对确实把选项挪出容器的改版仍有效，且有回归用例），但本轮的现场证据表明它不是本次的成因。

## 3.0.14

### 修复

- **已批阅视图（章节测验「已完成」）「能提取到题干、提取不到选项」**：真机回报「已提取 3 道题，其中 2 道选择题没提取到选项」，且当时**选项明明显示在页面上**、脚本已是最新版 —— 说明这不是渲染时序问题，而是**选项不在我们选中的题目容器内**。该视图的部分版本把选项列表（`ul.Zy_ulTop`）放在与 `.TiMu` **同级**的位置（两者同在外层单题块 `.singleQuesId` 里），于是容器内的所有候选（含语义兜底）全部落空，表现为「题目都在、选项一个都没有」。现在 `TiMuExtractor` 的选项解析改为逐级兜底：
  1. 题目容器内按原有候选取（**正常页面行为完全不变**）；
  2. 容器内一个都取不到时，到**同题的外层单题块**（`.singleQuesId` / `.questionLi` / `.mark_item`）用同一套候选再取一次 —— 只有该层确实只装一道题时才采用，避免把邻题的选项串进来；
  3. 仍取不到时，按页面自己标的 **qid**（`li[qid]` / `li[data-qid]`）在同题块 → 整篇文档内精确找回（qid 逐题唯一，不会串题）。
  - 兜底只在「容器内确实取不到」时触发；页面既没有 qid 线索、选项也确实不在容器内时**不会乱取**别的选项（有回归用例守住）。

### 新增

- **面板一键把「真机现场」带回来（复制诊断信息）**：结果里出现「选择题却没有选项」时，状态栏旁会多出一个「复制诊断信息」按钮，点一下即把各层文档的结构报告复制到剪贴板，粘贴给维护者即可。报告包含：每层文档（含同源 iframe）的 `readyState`、题目容器数量、cxsecret 作用域与解码表条目数、逐题的「题干来源选择器 + 9 个选项候选各自的命中数 + 容器与外层结构速览」、按 qid 全文档查找的选项节点数，以及提取链路自检（提取器 / 题数 / 选项总数 / 缺选项选择题数）。
  - 为什么需要它：离线快照是「渲染完成之后」的 DOM，「选项看得见却提取不到」这类结构差异在离线复现不出来。报告里「容器内选项计数全为 0、外层同题块却有 4」就是本次这个 bug 的签名，一次即可判定成因。
- **面板标题常驻显示脚本版本**：`学习通题目导出 v3.0.14`。`APP_VERSION` 此前从未出现在任何界面上，每次排查都要先问一遍版本号，也曾出现「用户停在旧版导致白跑一轮排查」的情况。

### 说明

- 「选项容器候选」收为单一来源：`timu-extractor.ts` 的 `TIMU_OPTION_SELECTORS` 与 `option-parser.ts` 的 `FALLBACK_OPTION_SELECTORS` 改为导出，解析与诊断报告共用同一份，改选择器只需要改一处。
- 诊断只读 DOM、不改页面，报告长度有上限（超长自动截断）；报告里的题干/选项文本走与提取相同的富文本管道，字体反爬的混淆文字会一并还原，便于核对。

### 验证

- 单测 **82 → 88 例全绿**：新增 `tests/dom-diagnostics.test.cjs` 3 例（结构正常时的候选命中数与链路自检、选项移出题目容器后报告能指出「容器内为 0、外层有选项」并给出 `li[qid]` 线索、无题目文档不抛错）；`tests/option-fallback.test.cjs` 新增 3 例（**选项被移出题目容器、仍在该题单题块内时仍能取到选项**、**选项被移出单题块时靠 qid 精确找回且不串到邻题**、**没有任何 qid 线索时不会乱取别的选项**）。
- 真实快照回归（`.workbuddy/verify-options.cjs`）全部通过：答题页 5 题 / 5 选择题带选项 / 20 选项；考试页面 56 题 / 36 / 144；外层壳 + 答题 iframe 嵌套标题「导论1」、20 选项；模拟「选项晚 300ms」采样 1158ms 补齐无回归。
- 诊断报告已在真实快照上跑通（`.workbuddy/diag-report.cjs` → `.workbuddy/report-real.txt`），题干与选项文本正常还原、各候选命中数一目了然。
- typecheck 与 ESLint（`src`）0 问题。

## 3.0.13

### 修复

- **章节测验/作业答题页「只有题干、没有选项」的最后一个时间窗口**：`settleQuestions()` 的分批渲染兜底原本是「选择题缺选项 + 连续稳定 3 秒 ⇒ 按现状返回」。但答题页是**一整个大 HTML**（实测 `/mooc-ans/api/work` 的答题页 423 KB），浏览器按到达顺序解析：题干容器 `.Zy_TItle`（结构里在前）可能已经入 DOM，而同一题的选项 `ul.Zy_ulTop`（结构里在后）还没解析到。网络/渲染卡顿时这个中间态可以稳住超过 3 秒，于是把「只有题干」的半成品当成了最终结果。现在只要还有**承载题目的文档**处于 `document.readyState === 'loading'`，就不允许按 3 秒兜底提前返回，继续采样到选项到达（`deadline` 仍然兜底，不会无限等）。
  - 行为边界：**结果完整（选择题都有选项）时的返回时机完全不变**；只有「选择题缺选项」的帧会多等；页面确实不渲染选项时仍在 `deadline` 内兜底返回最丰富的一帧。

### 说明

- 本轮把 `html/` 下现存的快照按真实嵌套关系又跑了一遍逐层诊断（外层壳 → 知识卡片页 → `/ananas/modules/work` 模块壳 → `/mooc-ans/api/work` 答题页，答题页位于**第 4 层**），**离线依旧无法复现「完全读不到选项」**：`单章节内iframe详细.html` 5 题（4 单选 + 1 多选）/ 20 选项、`考试页面.html` 56 题 / 144 选项、`章节练习完整页.html` 只有外层壳（该章节的答题页在快照里未加载，提取为 null）。因此本版修的是**分析后唯一站得住的时间窗口**，并保留了面板的「N 道选择题没提取到选项」告警用于区分「提取器没读到」与「导出链路」。

### 验证

- 单测 **81 → 82 例全绿**：新增「选项始终不到、但文档仍在加载时，不按兜底提前返回（继续等选项）」（选项在 3.2s 到达 → 仍拿到 4 个选项；旧实现会在 3s 交出只有题干的结果）；typecheck 与 ESLint（`src`）0 问题。
- 真实快照（`.workbuddy/verify-options.cjs`，已改为快照缺失自动跳过）：`单章节内iframe详细.html` 5 题 / 5 道选择题全带选项 / 20 选项；`考试页面.html` 56 题 / 36 / 144；外层壳 + 答题 iframe 嵌套标题取到章节名「导论1」、20 选项；模拟「选项晚 300ms 补上」采样 **1137ms** 补齐，无回归。

## 3.0.12

### 修复

- **已批阅视图的题号整列为空**：`parseLeadingNumber()` 只认「1.」「1、」这种带分隔符的前缀，而已批阅视图的题号节点是裸数字 `<i class="fl">1</i>`，解析结果恒为 `undefined`（实测输出 `第undefined题`）。现在「整串就是数字」也算题号；同时不放宽成「行首数字」，避免把以年份开头的题干（「2024 年…」）误判成题号。
- **选项容器类名改版时会整题丢选项**：语义兜底选择器缺 `li[role="option"]`。答题页的选项带 `role="radio"` / `role="checkbox"`，而已批阅视图的选项是 `<li class="clearfix" role="option"><i class="fl">A、</i><a class="fl">…</a></li>`——一旦 `ul.Zy_ulTop` / `ul.qtDetail` 两个类名同时失效，兜底就接不上，表现为「只提取到题干、提取不到选项」。现补上 `li[role="option"]` 与 `.qtDetail li`（都放在候选最末，前面的候选命中时不介入，行为不变）。
- **导出的 txt / md 里，每道题的题干与每个选项都连续重复两遍**：`text-formatter.ts` 与 `markdown-formatter.ts` 里对应的 `output +=` 各写了两遍（含错题汇总的题干行，共 6 处）。核对旧主脚本第 1425–1430、1532–1533 行，**旧脚本原本就是这样**，TypeScript 移植时按「保持输出完全一致」逐行照搬，属历史遗留。本次按用户确认去掉重复输出（Word 导出无此问题），输出因此与旧版脚本不完全一致。

### 优化

- **面板会把「选择题没提取到选项」直接说出来**：此前这类半成品结果只显示「已提取 N 道题」，用户无法判断是页面还没渲染完还是提取失败。现在结果里存在「选择题却没有选项」的题目时，状态栏改为警告、给出题目数，并提示等题目和选项都显示出来后重新提取一次。

### 验证

- 单测 **78 → 81 例，全绿**：新增「选项容器类名全部改版时靠 `li[role="option"]` 仍取到 4 个选项」、题号解析用例（`1.` / `12、` / 裸数字 `1` 生效，`2024 年…` 不生效）、以及「题干与每个选项各只输出一次」的导出回归；typecheck 与 ESLint（`src`）0 问题。
- 真实快照：已批阅视图（`html/dom.html`）题号由 `undefined` 变为 `1 / 2 / 3`，3 题 / 8 选项、标题「导论1」无回归；`单章节内iframe详细.html` 5 题 / 20 选项无回归；三层嵌套（外层壳 → 卡片页 → 已批阅视图）逐文档诊断各层选项齐全（新增 `.workbuddy/diag-layers.cjs`）。
- 导出实测：已批阅视图的 txt 由「题干、每个选项各两行」变为各一行；Word 导出路径无此问题。

## 3.0.12 已知边界

用户回报的现场是：**章节练习「已做完（批阅结果）」视图、面板题数正常、但选项这一层确实没读到**，且当时脚本版本为 **3.0.10 或更早**。离线用 `html/dom.html`（同一模板）跑当前代码，选项一直能正常提出（3 题 / 8 选项），因此：

- 若升级到本版后选项正常 → 说明此前缺的是 3.0.10 之后补齐的采样 / 兜底逻辑；
- 若仍缺 → 说明真机该页的选项结构与 `dom.html` 不同（或选项比题干晚渲染超过兜底等待）。此时请按 F12 在 Console 跑 `.workbuddy/capture-quiz-dom.js`（或像上次那样把该 iframe 的 DOM 存成 `html/dom2.html`），报告里「逐题概览」的选项数与「选项元素样本」即可直接定位。


## 3.0.11

### 说明

- 用 `html/dom.html`（章节测验「已完成 / 已批阅」视图的完整 body 快照：章节「导论1」，2 道单选 + 1 道判断）在离线环境复跑当前提取链路，**题目与选项本身都能正常提出**（3 题、8 个选项、我的答案、章节名齐全），说明该模板的选项结构已被覆盖。本版据此修掉这份模板暴露出来的**信息缺失**问题（错题标记、答案解析、题型分组继承）。

### 修复

- **已批阅视图的错题会整页漏掉**：该视图不输出「正确答案」文本，只在每题下用图标表示批阅结果（`span.marking_dui` 正确、`span.marking_cuo` 错误、`span.marking_bandui` 部分正确）。此前 `hasExplicitWrongMarker()` 不认识这些类名，而这类页面又没有可比较的答案文本，`isWrong` 恒为 `false`，**错题汇总里一道都不会出现**。现在把 `marking_cuo` 与 `marking_bandui`（部分正确同样需要复习）一并纳入错题标记。
- **已批阅视图的「答案汇总」全是「（未找到答案）」**：该视图没有正确答案文本，导出后参考答案为空、错题汇总也断不出标准答案。现在当页面拿不到正确答案文本、且该题批阅结果为**正确**（`.marking_dui`）时，用我的答案回填正确答案（批阅正确即作答与标准答案一致）；批阅错误 / 部分正确不回填，避免把错答案当成标准答案；页面本身有正确答案时行为完全不变。实测该快照的「答案汇总」由三项「（未找到答案）」变为 `1. D`、`2. A`、`3. 对`。
- **已批阅视图的答案解析取不到**：页面自身的「AI 讲解」脚本读的是 `.newAnswerBx .answerKeyBx .answerCon`，我们的候选里只有 `.analysisBx .answerCon` 形态。现按页面自身约定补齐该容器。
- **「我的答案」有兜底形态缺失**：补上页面自身使用的 `.newAnswerBx .myAllAnswerBx .myAnswerBx`（一次作答渲染多组答案时使用）。
- **「正确答案」容器形态不全**：补上页面自身使用的 `.newAnswerBx .correctAnswer .answerCon` 与 `.correctAnswerBx .correctAnswer` 两种写法。
- **题型分组标题继承实际失效（死代码）**：`TiMuExtractor` 只查 `.aiArea` **后代**里的 `.newTestType`，但已批阅视图的 `h3.newTestType` 是 `.aiArea` 的**前一个兄弟节点**，永远匹配不到，`currentType` 一直是 `null`。后果是题干里没有「【单选题】」这类标记时（例如只靠分组标题标明题型的填空题）整题被丢弃。现在新增 `resolveGroupType()`，先查后代、再沿兄弟节点向上找最近的分组标题。

### 验证

- 新增单元测试 `tests/yipi-view.test.cjs`（7 例）：`.qtContent` + `form` 内 `ul.Zy_ulTop` 的选项提取、`marking_cuo` / `marking_bandui` / `marking_dui` 的错题判定、兄弟节点分组标题的题型继承（题干无标记也能识别出填空题）、`correctAnswer` / `answerKeyBx` 两种答案解析容器、**批阅正确时回填正确答案且页面自带正确答案时不覆盖**。
- 单测 **71 → 78 例，全绿**；typecheck 与 ESLint（`src`）0 问题；构建产物 442,869 → **446,196 字节**，`@version` 与 `APP_VERSION` 均为 3.0.11。
- 真实快照端到端验证：
  - `.workbuddy/verify-yipi-view.cjs`（新增）：外层壳 `章节练习完整页.html` + 已批阅视图 `dom.html` 同源嵌套 → 标题「导论1」、3 题、8 个选项齐全；「答案汇总」由三项「（未找到答案）」变为 `1. D`、`2. A`、`3. 对`；该视图 3 题批阅结果均为 `marking_dui`，`isWrong` 正确地为 `false`；
  - `.workbuddy/verify-options.cjs`（回归）：`作业页面.html`（44 题 / 34 道选择题带选项 / 156 个选项）、`考试页面.html`（56 题 / 36 / 144）、`单章节内iframe详细.html`（5 题 / 20）**均无回归**，采样补齐选项仍是 1150ms。

### 待真机确认

本版修的是这份快照暴露出的**信息缺失**；「提取不到选项」在离线用这份快照**复现不出来**（选项本来就能提出）。`.workbuddy/capture-quiz-dom.js` 已补上已批阅视图的选择器命中与**逐题概览**（选项数 / 批阅标记 / 我的答案 / 题干摘要），若真机上仍缺选项，请在题目已显示出来的页面上跑一次该脚本并回传报告。

## 3.0.10

### 修复

- **构建出来的脚本 @version 一直是 v3.0.0**：油猴头 `@version` 与 `APP_VERSION` 此前只取 `package.json`，而它长期停在 `3.0.0`、CHANGELOG 已推进到 3.0.x，导致装了新版却无法从版本号判断手上是哪一版。现在 `tools/build.mjs` 改为**以 CHANGELOG.md 顶部条目的版本为准**（`package.json` 兜底），构建日志会打印实际写入的版本与来源；两者不一致时额外给出告警。同步把 `package.json` 提升到 `3.0.10`。
- **章节练习只提取到题干、提取不到选项**：`settleQuestions()` 原先「看见题目就收工」，而答题页是分批渲染的，中途被采样到时可能只渲染出题干、选项还没补上，于是把半渲染的结果当成了最终结果（单章节与多章节批量都会中招）。现在：
  - 新增 `incompleteChoiceCount()`：选择题（单选/多选）却没有选项即视为「明显未渲染完」；
  - `settleQuestions()` 要求结果**连续多帧完全一致**（`SETTLE_STABLE_MS = 600`）才判定渲染结束，选择题缺选项时继续等待补齐；
  - 全程记录「最丰富的一帧」（题数优先、其次选项总数），超时返回它，不再返回比中间帧更差的结果；
  - 页面确实渲染不出选项时按 `PARTIAL_SETTLE_MS = 3000` 兜底返回，避免个别页面白等满整段超时；
  - `selectBest()` 在题数相同时改为优先选**选项更完整**的候选，防止「只渲染了题干」的候选靠来源优先级把完整候选顶掉。
- `app-controller.resolveCurrentResult()` 与 `chapter-extraction-service` 的「章节已激活」快路径同样接入完整性判定：已经取到题目但选择题缺选项时不再直接采用，交给采样等待补齐。

### 优化

- `parseOptions()` 末尾追加**语义兜底选择器**（`.Zy_ulTop li`、`li[onclick*="addChoice"]`、`li[role="radio"]`、`li[role="checkbox"]`）。学习通 2026 版答题页的选项是 `<li class="font-cxsecret before-after" role="radio" onclick="addChoice(this)">`，类名随版本变化但 `role` 与 `onclick` 极稳定；正常页面仍走各提取器自带候选，行为不变。
- 移除已被 `settleQuestions()` 取代的 `utils/async.ts` 的 `poll()`（唯一调用点已删除）。

### 验证

- 新增单元测试 `tests/option-fallback.test.cjs`（4 例）：选项类名改版但保留 `role="radio"` / 只剩 `onclick="addChoice"` 时仍能取到选项、结构正常时兜底不介入、`incompleteChoiceCount()` 对选择题与填空题的判定。
- `tests/extraction-settle.test.cjs` 增补 3 例：**选项比题干晚 300ms 渲染时采样会把选项补齐**、选择题始终无选项时按兜底返回不拖满超时、结果指纹包含选项变化。并把「立即返回」的断言改为「在稳定窗口后返回」（实测 600–800ms，仍远低于旧实现的 14–20s）。
- 单测 **64 → 71 例，全绿**；typecheck 与 ESLint（`src`）0 问题；构建产物 437,950 → **442,869 字节**，`@version` 与 `APP_VERSION` 均为 **3.0.10**。
- 真实快照端到端验证（`.workbuddy/verify-options.cjs`）：`作业页面.html`（44 题，34 道选择题全部带选项，选项总数 156）、`考试页面.html`（56 题，36 道选择题全部带选项，144 个选项）、`单章节内iframe详细.html`（5 题，20 个选项）**均无回归**；用外层壳 `章节练习完整页.html`（10.9 MB）+ 答题 iframe 复现真实嵌套结构，标题取到章节名「导论1」、5 题 20 个选项；在真实 DOM 上模拟「选项晚 300ms 补上」，采样 **1133ms** 拿到完整选项（旧实现会在此刻交出 0 个选项）。

## 3.0.9

### 修复

- **标题仍显示不出章节名**：上版只认了 `#prev_title`（id），而真实 DOM 用的是类名 `<div class="prev_title" title="毛泽东思想的主要内容">`，且该节点在外层壳文档里，题目却位于嵌套的知识卡片 / 答题 iframe —— 嵌套文档只能解析出自己的标题（「章节测验 待完成」）。现在：
  - `resolvePrevTitle()` 同时匹配 `#prev_title` 与 `.prev_title`（title 属性优先，回退正文）；
  - `ExtractionService` 在选出候选结果后用外层壳的章节名覆盖标题（`applyScopeTitle()`），因此**题目在嵌套 iframe 里也能显示章节名**，并直接决定导出文件名与 Word 试卷标题。
- **标题不再参与题目指纹**（`ExtractionService.fingerprint()`）：章节名的更新早于题目 DOM，纳入指纹会把上一章残留的题目误判成本章内容。

### 优化

- **章节切换提速**：去掉「切一次卡 + 固定长等待」的节奏（旧流程先等任务卡栏 ≤2.5s，再固定等 14–20s），改为采样等待：
  - `ExtractionService.settleQuestions()` 每 120ms 采样一次跨层提取，**题目一出现立即返回**，检测延迟收敛到一个采样间隔；
  - 新增 `QuestionTabGuard`：每轮补切一次题目卡（单章最多 4 次、间隔 ≥400ms），解决「章节切换后卡片页重建、默认回到视频卡，只切一次的任务卡随旧页面失效」的问题；
  - 单次提取同样改为采样等待，并且不再先空等 4.5s 才去尝试切卡；页面确实没有题目卡时只等 4.5s。
- 移除已被取代的 `ExtractionService.waitForChangedResult()`（其「固定间隔长等待」语义与旧标题指纹规则已不再需要）。

### 验证

- 新增单元测试 `tests/extraction-settle.test.cjs`（6 例）：嵌套 iframe 场景下标题取外层壳章节名、无 `.prev_title` 时行为不变、题目就绪时采样立即返回（<300ms）、无题目时按超时结束、**章节切换后自动补切任务卡并秒级拿到题目**、章节已激活时不空等。`tests/task-tab-locator.test.cjs` 增补补切节流用例（限次 + 冷却）。
- 单测 **55 → 64 例，全绿**；typecheck 与 ESLint（`src`）0 问题；构建产物 434,555 → **437,908 字节**。
- 真实快照端到端验证（`.workbuddy/verify-prev-title.cjs`）：`html/章节测验.html` 提取标题由「章节测验 待完成」变为 **「毛泽东思想的主要内容」**（题数 5 不变）；`作业页面.html`（44 题，标题「章节测验 待完成」）与 `考试页面.html`（56 题，标题「学习通题目」）**无回归**；一次采样提取耗时 56–92ms（合并后的 11.3MB 巨型快照，真实页面各层文档更小）。

## 3.0.8

### 修复

- **不再需要手动切到「章节测验」**：知识卡片页默认停在「视频」卡，而任务卡内容是懒加载的 —— 卡片内部 iframe 的真实地址写在 `_src` 上，只有点击该卡触发页面自身的 `changeDisplayContent()` 之后才会写回 `src` 并加载答题页。因此不切卡时页面里根本不存在题目 DOM。现在提取前会自动定位并激活承载题目的任务卡（章节测验 / 作业 / 考试 / 练习），单次提取与批量提取都生效。
- **修复「一键提取多个章节」切不动章节**：`chapter-locator.activate()` 原先对容器 `div.posCatalog_select` 派发点击，但学习通把 `onclick` 挂在内部的 `span.posCatalog_name` 上，事件不会向下传递，章节实际没有切换，每章只能空等超时。现在优先选中带 `onclick` 的节点。
- **修复章节列表把分组标题当章节**：`.posCatalog_select` 会把第一级分组标题（`.firstLayer` / `.posCatalog_title`）一起收进来（真实快照 64 项里 10 项是分组标题）。现在优先取 `span.posCatalog_name` 并显式排除分组标题，快照实测 64 → 54 项。
- 章节名兜底补充 `textContent`：章节名节点不带 `title` 属性时不再退化成「第 N 章」。

### 新增

- 新增 `application/task-tab-locator.ts`：任务卡（任务点）定位器。跨同源 iframe 找到 `#prev_tab`，按文案匹配题目类任务卡，返回 `active` / `switched` / `missing` 三态（已激活 / 本次切换 / 页面无可用任务卡）。
- `utils/dom.ts` 新增 `collectAccessibleDocuments()`：跨层收集可同步访问的文档（含同源 iframe）。章节与任务卡定位、`ExtractionService` 共用同一套 iframe 遍历规则（后者改为复用，行为不变）。
- 批量提取流程升级为「切章节 → 等任务卡栏重建 → 切题目卡 → 等题目 → 提取」，收尾按「先还原章节、再还原任务卡」恢复用户原有浏览状态；任务卡栏只在知识卡片页存在，因此批量开始前探测一次，避免逐章空等。
- 批量结果标题规则明确为：单章节直接用章节名，多章节用「页面标题 - N 个章节」（页面标题已内含 `#prev_title` 优先级）。

### 验证

- 新增单元测试 `tests/chapter-locator.test.cjs`（5 例）与 `tests/task-tab-locator.test.cjs`（7 例）：覆盖分组标题排除、激活态识别、点击目标必须是带 `onclick` 的节点、任务卡三态、文案回退（`title` → `spanText` → 去序号的正文）、同源 iframe 内定位。
- 单测 **43 → 55 例，全绿**；typecheck 与 ESLint（`src`）0 问题；构建产物 421,223 → **434,555 字节**。
- 真实快照 `html/章节测验.html`（11.3 MB，多层 iframe 合并）端到端验证（`.workbuddy/verify-chapter-flow.cjs`）：目录 64 个容器 → 精确识别 54 个章节（等于 `span.posCatalog_name` 节点数，差额正好是 10 个分组标题）；3 个任务卡栏全部解析正确（默认停在视频卡的栏返回 `switched`，已停在章节测验的栏返回 `active`）；`activate()` 的点击目标确认为 `SPAN.posCatalog_name`（带 `onclick`），不再是容器 div。
- 待真机确认：`changeDisplayContent` 的函数定义不在快照中（外链 JS），「切卡把 `_src` 写回 `src`」这条链路依据的是页面自身的懒加载约定与 `#frame_content` 的实际取值，需在真实页面最终确认。

## 3.0.7

### 新增

- 学生学习页面（章节练习 / 章节测验 / 做作业）提取的标题优先取自 `#prev_title` 容器：先取其 `title` 属性，缺失时回退到其正文。该容器承载章节/测验的真实名称，比浏览器默认的 `document.title`（多为课程名或「超星学习通」）更准确，导出文件名与 Word 试卷标题据此生成。无 `#prev_title` 时仍走原有选择器与 `document.title` 兜底，其他页面行为不变。

### 验证

- 新增单元测试 `tests/page-context.test.cjs`（3 例）：覆盖「`#prev_title` 的 title 属性优先」「无 title 属性回退正文」「无 `#prev_title` 时原有逻辑不变」。
- 单测 **38 → 41 例，全绿**；typecheck 与 ESLint 0 问题；构建产物已更新。

## 3.0.6

### 修复

- 修复「章节测验 / 作业页面」题干与选项导出为乱码的问题。学习通对答题页启用了 `font-cxsecret` 字体反爬：把部分汉字换成「备用码位」，再用内联字体把该码位画成真字的轮廓，因此人眼所见正常而 `textContent` 读出乱码（如「中悢共产党悤史上」）。原先提取器读到什么就导出什么，导出的题干、选项文字不可用。诊断与验证过程见 `docs/chapter-quiz-diagnosis.md`。

### 新增

- 新增 `extractors/cxsecret-font.ts`：字体反爬解码的纯逻辑层。利用「子集字体的 `glyf` 表逐字节保留了源字体字形数据」这一特性，用
  `sha1(glyf[loca[gid] : loca[gid + 1]])[:4]` 反查真实汉字码位。内含自实现的同步 SHA-1（只取摘要首 4 字节）与最小 sfnt 解析（`head`/`maxp`/`loca`/`glyf`/`cmap`，支持 cmap format 4 与 12、支持 TTC）。
- 新增 `extractors/cxsecret-table.ts`（生成物）：`sha1[:4] → BMP 码位` 解码表，覆盖 U+4E00–U+9FA5 共 20902 字，4 字节键经生成器校验**零冲突**，二进制紧凑存放（122.5 KB 裸数据 / base64 163.3 KB），运行时二分查找。
- 新增 `extractors/cxsecret-decoder.ts`：DOM 接入层。从文档的 `<style>`（及同源 `styleSheets`）中收集内联 `data:` 字体，构建映射并按文档缓存；**只还原处于 cxsecret 作用域内的文本**（自身或祖先类名含 `cxsecret`），避免把作用域外的同码位正常文字改坏；页面若把该类名改掉则退化为整页还原。家族名不匹配时回退尝试全部内联字体，且要求命中数达标，避免误伤普通内嵌字体。
- 新增 `tools/gen-cxsecret-table.py`：解码表生成器，支持两条路径 —— `--font <字节兼容的源字体>`（只依赖 OFL 字体，推荐）与 `--pkl <目录>`（用预生成哈希表重建），生成时校验键冲突。
- `extractors/rich-content.ts` 接入解码：文本节点按其所属元素作用域还原，`aria-label` 等属性兜底路径同样还原（真实页面的选项文字同时存在于这两处）。
- 新增「提取多个章节」相关的解码覆盖：多层 iframe 各自持有独立文档与内联字体，解码器按文档缓存，互不干扰。
- 新增单元测试 `tests/cxsecret-decoder.test.cjs`（11 例）与字体夹具 `tests/fixtures/cxsecret-quiz.ttf`（真实章节测验页内联字体，20,756 字节）：覆盖 SHA-1 与 node crypto 一致、内置表条目与冲突、base64 解码、真实字体 83 条映射、作用域内外差异、无字体/字体损坏/类名被改的退化行为、`extractRichContent` 与 `TiMuExtractor` 端到端还原。

### 验证

- 单测 **27 → 38 例，全绿**；typecheck 与 ESLint 0 问题。
- 解码正确性以两条**互相独立**的方法交叉验证：字形哈希反查 vs 栅格形状匹配，`sha1[:4]` 结果与栅格真值 **83/83、154/154 完全一致**。
- 三份真实快照端到端：`html/章节测验.html`（四层嵌套）解出 83 条映射、提取 **5 题**且题干选项可读；`html/作业页面.html` 解出 154 条映射、提取 **44 题**；`html/考试页面.html`（无混淆）正确判定为无需解码、提取 **56 题**。
- 构建产物 227,687 → **420,573 字节**（增量主要为 163 KB 解码表）。浏览器冒烟测试失败点与改动前基线相同（既有的 `navigator.clipboard` 环境问题），提取链路无回归。

## 3.0.5

### 修复

- 修复「做作业 / 章节测验」答题页提取不到题目（面板提示「当前页面未识别到题目」）的问题。题干容器选择器只匹配 `.qtContent / .question-content / .mark_name`，而 2026 版答题页的题干在 `.Zy_TItle > div.fontLabel`，选择器落空 → 题干为空 → `createQuestion` 据此判定整题无效并丢弃 → 四类提取器全部返回 0 题。诊断过程与页面结构证据见 `docs/chapter-quiz-diagnosis.md`。

### 新增

- 新增 `extractors/question-stem.ts`：题干**分层定位**，取代四个提取器各自硬编码的单一选择器列表，以应对学习通频繁改版：
  1. 已知题干类名（新版 → 历史版本，集中在 `STEM_CONTAINER_SELECTORS` 一处维护）
  2. 题型标记的父元素（题干与「【单选题】」同级，题干容器改名也不受影响）
  3. 兜底：剔除题号、选项列表与答案区后，取标题容器 / 整题容器的剩余内容
  日后改版导致题干丢失时，只需追加一个类名，无需改动任何提取流程代码。定位结果同时返回「已剥离前缀的题干」与「原始文本」，题号解析结果与改动前一致。
- `mark-item-extractor` / `question-li-extractor` / `generic-exercise-extractor` 同步改用该定位器，删除各自重复的选择器列表。
- 新增提取器单元测试 `tests/question-stem.test.cjs`、`tests/question-extraction.test.cjs`（共 13 例），覆盖 2026 版真实结构、题干类名被改、题型标记被改、兜底不混入选项文本、图片题干、空题干不产出无效题目等场景。
- 引入仅测试使用的 `linkedom`（devDependency）为 Node 单测提供 DOM 环境；`tsconfig.test-build.json` 的 include 扩展为 `src/extractors/**/*.ts`，使提取器首次进入单测覆盖范围。构建产物不受影响。

## 3.0.4

### 功能

- 文件名预览优化：勾选「附加答案」时，文件名预览区自动追加「（含答案）」后缀；取消勾选或勾选「题库导入」时自动移除/切换，与主脚本 v1.7 行为一致。`updateFilenamePreview()` 在选项变化与提取完成时刷新，历史恢复后按恢复的勾选状态重新计算。
- 导出格式为 TXT/MD 时，「题库导入」选项保持始终禁用（灰显）并自动取消勾选，切回 Word 格式恢复可用。
- 历史记录弹窗 UI 对齐主脚本：弹窗改为 400px 白卡片（浅色）/ Catppuccin 深色卡片，列表项显示「时间 · 题数 · 格式 · 含答案/含错题/打乱/题库导入」标志，右侧提供圆形「重新下载 ⤓」「删除 ✕」图标按钮；点击条目本身恢复记录，点击图标分别触发重新下载/删除（与主脚本交互一致）。`panel-view.ts` 模板与 `renderHistory()` 重写，`styles.ts` 新增 `cwe-history-*` 样式。
- 历史记录弹窗宽度增至 460px。

### 新增

- 「打开窗口自动提取」设置（默认关闭）：开启后脚本加载时延迟 800ms 自动执行一次提取，后续页面变化不再自动提取。`UserSettings` 新增 `autoExtractOnLoad` 字段，`settings-repository` 兼容旧配置（缺省回落为关闭），设置弹窗新增开关行。

## 3.0.3

### 修复

- 修复 Word 导出时题目与选项之间出现多余空行的问题（与主脚本 v2.0.2 同步修复）：学习通题干 HTML 末尾常残留「换行 + 纯空白文本」，`normalizeRichContent` 只修剪首尾换行、漏掉被空白文本挡住的换行，Word 中渲染为段内空行；现改为首尾的换行与纯空白文本一并修剪（图片前的换行仍保留）。`extractors/rich-content.ts` 与 `exporters/legacy-bridge.ts` 两处同步修改，并新增单元测试覆盖。

## 3.0.2

### 修复

- 提取结果不含参考答案时，导出选项区自动隐藏「附加参考答案」和「附加错题汇总」两项，并强制取消勾选（含从历史记录恢复偏好的场景）；重新提取到含答案的题目后恢复显示。

## 3.0.1

### 重构

- 导出与下载逻辑改为直接移植主脚本 `chaoxing-work-export.user.js` 的实现，输出与主脚本完全一致：
  - TXT/Markdown 格式化（含题干与选项的双行输出、答案汇总、错题汇总）逐行移植。
  - Word 生成（`generateWordBlob` 全套：题库智能导入格式、试卷排版、答案页、错题汇总、图片按比例缩放与失败计数）逐行移植。
  - 打乱逻辑改回原版 `shuffleQuestions`（题型内 Fisher-Yates 打乱；题目与答案随打乱顺序生成，错题汇总始终用原始顺序）。
  - 下载方式对齐原版（临时 `<a>` 点击后立即释放 URL，多文件间隔 300ms）。
  - 下载/复制状态提示文案对齐原版（如「题库导入格式已下载」「（N 张图片加载失败）」「Word 格式不支持复制，请使用下载」）。
- 新增 `exporters/legacy-bridge.ts`：主脚本数据结构定义、领域模型到原版结构的转换，以及原版公共辅助函数（`questionContent`/`optionContent`/`answerContent`/`formatRichForText`/`formatRichForMD` 等）。
- 删除 `exporters/result-transformer.ts`（被原版打乱逻辑替代）。

### 修复

- 修复 `tools/test.mjs` 在 Windows 下调用 `tsc.cmd` 触发 EINVAL 的问题（改为 node 执行 tsc，与 build.mjs 一致）。
- 补充根 `tsconfig.json`，修复 ESLint projectService 无法定位项目导致 lint 失败的问题。

## 3.0.0

### 重构

- 将单文件 JavaScript 重构为严格 TypeScript 多模块工程。
- 建立 domain、extractors、application、exporters、infrastructure、ui 六层结构。
- 构建阶段打包为单个可安装 userscript。
- 使用 Shadow DOM 隔离界面样式。
- 将 iframe 通信升级为带协议版本和来源校验的结构化消息。
- 将章节发现、章节遍历与单页解析拆分。
- 将 Word、TXT、Markdown 统一到同一题目领域模型。
- 增加版本化设置与历史仓储以及旧数据迁移。

### 功能

- 增加答案解析提取和导出。
- 增加通用练习页面兜底适配器。
- 增加按章节拆分文件。
- 增加 CSS 背景图片和文本填空输入提取。
- 增加图片请求缓存、超时和失败统计。
- 保留参考答案、错题汇总、题型内随机排序和题库智能导入格式。

### 工程化

- TypeScript strict 配置。
- ESLint、Prettier、EditorConfig。
- Node.js 单元测试。
- GitHub Actions 校验与构建产物上传。
- Dependabot 依赖更新。
- 架构、贡献、解析器扩展和安全文档。
