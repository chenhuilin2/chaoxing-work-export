# Security Policy

## 支持版本

仅维护最新主版本。安全修复发布后，请升级到最新构建产物。

## 报告安全问题

请通过仓库维护者提供的私密联系方式报告可导致以下影响的问题：

- 题目或账号数据被意外发送到第三方。
- 未经用户操作执行任意脚本或下载。
- `postMessage` 消息可被不可信来源利用。
- 文件名、富文本或导出内容导致代码执行。

公开 issue 中不得粘贴 Cookie、Token、账号、真实学生数据或未授权题库。

## 安全设计

- 业务数据默认只存在于页面内存与本地 localStorage。
- iframe 消息包含协议版本并校验来源域。
- UI 文本通过 `textContent` 写入，不把题目内容拼接为 HTML。
- 下载文件名经过非法字符和长度清洗。
- 图片请求设置超时，单张失败不会阻塞整个导出。
- 依赖版本固定，并由 Dependabot 监控开发依赖更新。
